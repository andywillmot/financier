version: '3.7'

services:
  web:
    build: ./mysite
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - ./mysite/:/usr/src/mysite/
    ports:
      - 8000:8000
    env_file:
      - ./.env.dev